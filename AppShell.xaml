<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="IscaCar.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:IscaCar.View">

    <TabBar>
        <ShellContent Title="BuscarPage"
                      ContentTemplate="{DataTemplate local:BuscarPage}" />
        <ShellContent Title="ViajePage"
                      ContentTemplate="{DataTemplate local:ViajePage}" />
        <ShellContent Title="PerfilPage"
                      ContentTemplate="{DataTemplate local:PerfilPage}" />
    </TabBar>
</Shell>